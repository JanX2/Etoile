{
    IBClasses = (
	{
	    ACTIONS = {
		searchAction = id;
	    };
	    CLASS = AppController;
	    LANGUAGE = ObjC;
	    OUTLETS = {
		dictionaryContentWindow = id;
		searchResultView = id;
	    };
	    SUPERCLASS = NSObject;
	},
	{
	    ACTIONS = {
		searchAction = id;
	    };
	    CLASS = FirstResponder;
	    LANGUAGE = ObjC;
	    SUPERCLASS = NSObject;
	}
    );
    IBVersion = 1;
}