{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {changePresence = id; click = id; remove = id; }; 
            CLASS = RosterController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                avatarColumn = NSTableColumn; 
                column = NSTableColumn; 
                presenceBox = NSPopUpButton; 
                statusBox = NSTextField; 
                view = NSOutlineView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}