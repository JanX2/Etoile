{
    IBClasses = (
        {
            CLASS = FMController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                fontList = NSOutlineView; 
                groupList = NSTableView; 
                sampleController = FMSampleController; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = FMSampleController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                backgroundColorWell = NSColorWell; 
                customSampleField = NSComboBox; 
                foregroundColorWell = NSColorWell; 
                sampleView = NSTextView; 
                sizeField = NSTextField; 
                sizeListView = NSTableView; 
                sizeSlider = NSSlider; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}