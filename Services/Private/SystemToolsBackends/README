===================
SystemToolsBackends
===================

The System Tools Backends are a set of cross-platform scripts 
for Linux and other Unix systems. The backends provide 
an standard XML interface for modifying the configuration 
regarless of the distribution that's being used.

to extract the configuracion
----------------------------

backend --get

to restore the configuration from XML
-------------------------------------

backend --set < file.xml
