#Make a temporary directory in order to have library/bundle compiled

before-all:: 
	if [ ! -h "OgreKit" ]; then \
	  $(LN_S) Headers OgreKit; \
	fi;

after-clean::
	if [ -h "OgreKit" ]; then \
	  rm OgreKit; \
	fi;

