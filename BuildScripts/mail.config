#!/bin/sh

#
# Mail profile for MAIL_SCRIPT provided in testbuild.config
#

export MAIL_FROM=etoile-testbuild@gna.org
export MAIL_SMTP_SERVER=smtp.free.fr
export MAIL_SMTP_USERNAME=
export MAIL_SMTP_PASSWORD=

# For GMail, some extra options are required
#export MAIL_OPTIONS='-o tls=yes'
